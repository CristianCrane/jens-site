# default application environment variables. This is committed to the repo, do not place secrets here.

# `VITE_*` are exposed to both client and server
VITE_APP_NAME=Empire Cleaning & Pro Services

# exposed to server only
DATABASE_URL=postgresql://user:password@localhost:5432/myapp_local